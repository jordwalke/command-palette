{
  "name": "quick-palette",
  "version": "0.39.2",
  "main": "./lib/quick-palette-view",
  "description": "Command Palette with ability to open faster. Use `cmd-shift-p` (macOS) or `ctrl-shift-p` (Linux/Windows).",
  "activationCommands": {
    "atom-workspace": [
      "quick-palette:toggle"
    ]
  },
  "repository": "https://github.com/jordwalke/command-palette",
  "license": "MIT",
  "engines": {
    "atom": "*"
  },
  "dependencies": {
    "atom-space-pen-views": "^2.0.0",
    "fuzzaldrin": "^2.1.0",
    "fuzzaldrin-plus": "^0.1.0",
    "underscore-plus": "^1.0.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  }
}
